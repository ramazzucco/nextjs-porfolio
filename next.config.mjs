const nextConfig = {
    basePath: '/nextjs-porfolio',
    reactStrictMode: true,
    swcMinify: true,
    images: {
        unoptimized: true,
    },
    output: 'export',
};

export default nextConfig;
